<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.foi.air.food2go">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StanjeBodovaActivity"></activity>
        <activity
            android:name=".GlavniActivity"
            android:label="@string/title_activity_glavni"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".controller.AktivacijskiKodActivity" />
        <activity android:name=".controller.RegistracijaActivity" />
        <activity android:name=".controller.ZaboravljenaLozinkaActivity" />
        <activity android:name=".controller.LogInActivity" />
        <activity android:name=".OdabirPotkategorijeActivity"></activity>
        <activity android:name=".OdabirKategorijeActivity" />
        <activity android:name=".GlavniZaslonActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
